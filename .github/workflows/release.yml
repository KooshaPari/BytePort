name: Release

on:
  push:
    branches: [main, master]
    tags: ['v*']
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: read

jobs:
  # On tag: build and upload Tauri release artifacts
  tauri-build-and-release:
    if: startsWith(github.ref, 'refs/tags/v')
    timeout-minutes: 60
    name: Build Tauri release artifacts (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
          - os: macos-latest
          - os: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/web
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4

      - name: Install Node.js
        uses: actions/setup-node@4a3601121dd01d1626a1e23e37211e3254c1c06c # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/web/package-lock.json

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1482605baf623a1a564a94d05b19b06a48596c13 # stable

      - name: Install frontend dependencies
        run: npm ci

      - name: Build release artifacts
        uses: tauri-apps/tauri-action@8eb9fe97038a45d8f08df27bf6d4c0a1da6e00f8 # v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: frontend/web/src-tauri
          tauriScript: npm run tauri -- build
          includeRelease: true
          includeDebug: false

  # On push to main: refresh release draft
  draft:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 30
    name: Update release draft
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Update release draft
        uses: release-drafter/release-drafter@693d20e7c1ce1a81d3a41962f85914253b518449 # v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
