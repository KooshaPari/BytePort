version: "3.8"
services:
  backend:
    build: .
    target: backend
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=sqlite:///data/byteport.db
  web:
    build: .
    target: desktop
    ports:
      - "3000:3000"
