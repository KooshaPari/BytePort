# Implementation Strategy

Keep the repair in an isolated worktree because canonical BytePort has unrelated
local workflow, SPEC, module, and backend edits. Limit downstream changes to
README disclosure and session evidence.
