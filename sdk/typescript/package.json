{
  "name": "@byteport/client",
  "version": "0.1.0",
  "description": "BytePort TypeScript SDK — typed client for BytePort API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write src/",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["byteport", "deployment", "cloud", "api-client"],
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
